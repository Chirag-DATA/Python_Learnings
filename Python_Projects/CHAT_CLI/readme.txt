Concept :->
       Two users (say User1 and User2) will chat using a shared text file.

Each new message will be appended to that file with:
       ->   Username
       ->   Timestamp
       ->   Message
When a user wants to see the chat, the program reads the file and prints all messages.